// Top-level build file. The web game in the repo root is NOT built by
// Gradle — this project only produces the thin Android TV shell.
plugins {
    id("com.android.application") version "8.13.2" apply false
    id("org.jetbrains.kotlin.android") version "2.3.21" apply false
}
