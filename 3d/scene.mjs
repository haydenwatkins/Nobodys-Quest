import {makeRunner,buildHomes,buildAuthoredWorld,placeArt} from './art.mjs?v=20260907-battle';
import * as T from './vendor/three.module.min.js';
import {RADIUS,LANDMARKS,STRUCTURES,WEAPONS,ENEMIES,DECORATIONS,landHeight,roadDistance,random,clamp} from './world-data.mjs?v=20260907-battle';
const UP=new T.Vector3(0,1,0),V=new T.Vector3();
const GEO={box:new T.BoxGeometry(1,1,1),ball:new T.IcosahedronGeometry(1,0),soft:new T.IcosahedronGeometry(1,1),cone:new T.ConeGeometry(1,1,5),cyl:new T.CylinderGeometry(1,1,1,8),ring:new T.TorusGeometry(1,.06,5,32)};
// Uneven, broad-sided outcrops share one geometry for instancing.
GEO.rock=new T.CylinderGeometry(.72,1,1,7,1,false).toNonIndexed();
{const p=GEO.rock.attributes.position;for(let i=0;i<p.count;i++){const x=p.getX(i),y=p.getY(i),z=p.getZ(i);p.setXYZ(i,x+y*.19,y+(y>0?.07*Math.sin(x*6+z*3):0),z+y*.08);}GEO.rock.computeVertexNormals();}
const mats=new Map();
function mat(color,extra={}){const key=color+JSON.stringify(extra);if(!mats.has(key))mats.set(key,new T.MeshStandardMaterial({color,roughness:.78,flatShading:true,...extra}));return mats.get(key);}
function g(parent){const o=new T.Group();if(parent)parent.add(o);return o;}
function part(parent,type,color,x,y,z,sx=1,sy=sx,sz=sx,rot=null){const o=new T.Mesh(GEO[type],mat(color));o.position.set(x,y,z);o.scale.set(sx,sy,sz);if(rot)o.rotation.set(...rot);o.castShadow=true;o.receiveShadow=true;parent.add(o);return o;}
function glow(parent,type,color,x,y,z,sx=1,sy=sx,sz=sx,rot=null){const m=part(parent,type,color,x,y,z,sx,sy,sz,rot);m.material=mat(color,{emissive:color,emissiveIntensity:1.6,roughness:.3});return m;}
export function surface(x,z,alt=0){const d=Math.hypot(x,z),a=d/RADIUS,r=RADIUS+landHeight(x,z)+alt,s=d?Math.sin(a)/d:0;return new T.Vector3(x*s*r,Math.cos(a)*r,z*s*r);}
function place(o,x,z,alt=0,yaw=0){o.position.copy(surface(x,z,alt));o.quaternion.setFromUnitVectors(UP,o.position.clone().normalize());o.rotateY(yaw);}
function rod(parent,a,b,r,color){const va=new T.Vector3(...a),vb=new T.Vector3(...b),delta=vb.clone().sub(va),mesh=part(parent,'cyl',color,...va.add(vb).multiplyScalar(.5).toArray(),r,delta.length(),r);mesh.quaternion.setFromUnitVectors(UP,delta.normalize());return mesh;}
function bladeGeometry(){const s=new T.Shape();s.moveTo(-.16,0);s.lineTo(-.25,1.65);s.lineTo(.13,2.5);s.lineTo(.35,1.42);s.lineTo(.19,0);s.closePath();const geo=new T.ExtrudeGeometry(s,{depth:.12,bevelEnabled:false});geo.rotateX(Math.PI/2);return geo;}
const BLADE=bladeGeometry();
function weapon(parent,id){const w=g(parent);w.position.set(.04,-.66,.16);w.rotation.x=-.65;
 if(id==='shear'){part(w,'cyl',0x263642,0,0,-.25,.09,.65,.09,[Math.PI/2,0,0]);part(w,'box',0xbda071,0,0,.05,.68,.14,.18);const blade=new T.Mesh(BLADE,mat(0xc5cfca,{metalness:.7,roughness:.34}));w.add(blade);glow(w,'box',0xff9c56,.17,-.01,.93,.05,.04,1.7);part(w,'box',0x3e5960,-.15,0,.5,.17,.17,.9);}
 else if(id==='pike'){rod(w,[0,0,-.8],[0,0,2.7],.085,0x42515a);part(w,'box',0xd0af73,0,0,2.45,.36,.28,.6);glow(w,'cone',0x70f0ef,0,0,3.08,.22,.9,.2,[Math.PI/2,0,0]);for(const s of [-1,1])part(w,'box',0x718c93,s*.22,0,2.7,.08,.16,.65,[0,s*.17,0]);}
 else{rod(w,[0,0,-.4],[0,0,1.9],.13,0x695846);part(w,'box',0x263f4c,0,0,1.95,1.5,.72,.74);for(const s of [-1,1]){part(w,'box',0xc7a166,s*.64,0,1.95,.22,.83,.88);glow(w,'box',0xffbe67,s*.77,0,1.95,.04,.58,.53);}part(w,'cyl',0x627d86,0,0,1.95,.23,1.05,.23,[0,0,Math.PI/2]);}
 return w;
}
export function makeHero(id='shear',npc=false){return makeRunner(weapon,id,npc);}
export function makeEnemy(type){const root=g(),body=g(root),legs=[];
 if(type==='mite'){
  part(body,'ball',0x566474,0,.7,0,.75,.48,.85);part(body,'box',0xb78353,0,.87,.25,.65,.25,.72);glow(body,'box',0xf29760,0,.73,.84,.45,.12,.05);
  for(const s of [-1,1]){for(const z of [-.45,.4]){const l=g(body);l.position.set(s*.58,.7,z);rod(l,[0,0,0],[s*.56,-.25,.14],.06,0xb69b71);rod(l,[s*.56,-.25,.14],[s*.65,-.64,.34],.055,0x324655);legs.push(l);}part(body,'cone',0xb3b6a1,s*.38,.48,1.13,.15,.9,.11,[Math.PI/2,0,s*.3]);}
 }else if(type==='dredger'){
  part(body,'box',0x324957,0,1.1,0,1.3,1.7,1);part(body,'cyl',0x9c794e,0,2.01,0,.64,.45,.58);glow(body,'box',0xee9260,0,1.88,.6,.76,.14,.05);
  for(const s of [-1,1]){const leg=g(body);leg.position.set(s*.62,.9,0);part(leg,'box',0x526776,0,-.35,0,.38,.65,.45);part(leg,'box',0x213a4a,0,-.7,.16,.6,.3,.85);legs.push(leg);rod(body,[s*.8,1.7,0],[s*1.1,.8,.5],.15,0xb39461);part(body,'box',0x7e8e94,s*1.1,.45,.68,.5,1,.35);}
 }else if(type==='kite'){
  part(body,'ball',0x4a6573,0,1.7,0,.6,.54,.8);for(const s of [-1,1]){part(body,'box',0x91a8ad,s*.91,1.8,0,1.3,.12,.7,[0,0,-s*.28]);part(body,'cone',0xbca075,s*1.52,1.6,.14,.17,.85,.2,[Math.PI/2,0,0]);}glow(body,'ball',0xffb678,0,1.69,.77,.19,.16,.05);part(body,'box',0x263f51,0,1.15,-.35,.23,.8,.33);
 }else if(type==='harrow'){
  part(body,'box',0x233e53,0,2.8,0,3.2,1.45,2.1);part(body,'cone',0x718996,0,3.7,-.4,.6,2.2,1,[.35,0,0]);
  const rotor=g(body);rotor.position.set(0,2.9,1.2);part(rotor,'ring',0xbda373,0,0,0,1.55);glow(rotor,'ball',0x86f6f0,0,0,.1,.48);for(let i=0;i<6;i++){const a=i*Math.PI/3;part(rotor,'box',0x7898a5,Math.sin(a)*.95,Math.cos(a)*.95,0,.32,1.05,.18,[0,.3,-a]);}root.userData.rotor=rotor;
  for(const side of [-1,1]){part(body,'box',0x456b81,side*2.5,2.9,-.3,3.2,.3,1.1,[0,side*.35,side*.2]);part(body,'cone',0xb99567,side*3.6,2.3,.1,.35,2.1,.5,[.3,0,side*.45]);part(body,'box',0x28475c,side*1.3,1.7,.6,.5,1.1,1.7);glow(body,'cone',0xffac68,side*1.3,1.5,1.6,.18,.6,.18,[Math.PI/2,0,0]);}
 }else{
  part(body,'ball',0x344b58,0,2.6,0,2.4,1.55,3.1);part(body,'box',0x8c7253,0,3.7,-.15,2.6,.4,3.9);part(body,'box',0x233b4d,0,2.5,2.8,2.3,.9,.8);glow(body,'box',0xffbc6a,0,2.63,3.25,1.5,.19,.06);
  for(const s of [-1,1]){for(let i=0;i<3;i++){const leg=g(body);leg.position.set(s*1.8,2.5,(i-1)*1.6);rod(leg,[0,0,0],[s*1.5,-.5,.2],.22,0xb19360);rod(leg,[s*1.5,-.5,.2],[s*1.9,-2.4,.7],.17,0x425f70);part(leg,'cone',0x759198,s*1.9,-2.45,.7,.3,.7,.45,[0,0,-s*.3]);legs.push(leg);}rod(body,[s*1.5,2.7,2],[s*2.2,2,3.8],.27,0xa78255);part(body,'box',0x385566,s*2.2,1.5,4.1,.75,2,1.4);glow(body,'box',0xfda65b,s*2.2,1.4,4.83,.5,1.4,.045);}
  for(let i=0;i<5;i++){part(body,'box',0x657783,0,3.85+i*.04,(i-2)*.7,1.35,.25,.4);glow(body,'box',0xf4bc75,0,4.01+i*.04,(i-2)*.7,.55,.04,.3);}part(body,'cone',0xaeb6a0,0,4.5,-2,.6,1.8,.4,[.5,0,0]);
 }
 root.userData={...root.userData,body,legs,type};return root;}
function batch(root,scene){root.updateMatrixWorld(true);const buckets=new Map();root.traverse(o=>{if(!o.isMesh)return;const key=o.geometry.uuid+o.material.uuid;if(!buckets.has(key))buckets.set(key,{geo:o.geometry,mat:o.material,transforms:[]});buckets.get(key).transforms.push(o.matrixWorld.clone());});for(const b of buckets.values()){const m=new T.InstancedMesh(b.geo,b.mat,b.transforms.length);b.transforms.forEach((v,i)=>m.setMatrixAt(i,v));m.castShadow=true;m.receiveShadow=true;m.computeBoundingSphere();scene.add(m);}}
function crescent(color,reach,combo,spread=1.15){const vertices=[],count=22;for(let i=0;i<count;i++){const a=-spread+i/count*spread*2,b=-spread+(i+1)/count*spread*2;const r=reach*(.83+.08*Math.sin(i/count*Math.PI)),inner=r-.65;for(const [ang,rad]of [[a,r],[a,inner],[b,r],[b,r],[a,inner],[b,inner]])vertices.push(Math.sin(ang)*rad,.9+Math.cos(ang)*.18,Math.cos(ang)*rad);}const geo=new T.BufferGeometry();geo.setAttribute('position',new T.Float32BufferAttribute(vertices,3));return new T.Mesh(geo,new T.MeshBasicMaterial({color,transparent:true,opacity:.9,side:T.DoubleSide,depthWrite:false}));}
export class WorldView {
 constructor(canvas,game,{renderer=null}={}){
  this.game=game;this.renderer=renderer||new T.WebGLRenderer({canvas,antialias:true,powerPreference:'high-performance'});this.renderer.outputColorSpace=T.SRGBColorSpace;this.renderer.toneMapping=T.ACESFilmicToneMapping;this.renderer.toneMappingExposure=1.05;this.renderer.shadowMap.enabled=true;this.renderer.shadowMap.type=T.PCFSoftShadowMap;
  this.scene=new T.Scene();this.scene.background=new T.Color(0xadb7a8);this.scene.fog=new T.Fog(0xbfc5b1,65,195);this.camera=new T.PerspectiveCamera(43,1,.1,700);this.yaw=-.22;this.zoom=1;this.overview=false;this.started=false;this.elapsed=0;this.shake=0;
  this.scene.add(new T.HemisphereLight(0xd7e4ce,0x4d5740,2.0));this.sun=new T.DirectionalLight(0xffe4b6,3.0);this.sun.position.set(-60,150,80);this.sun.castShadow=true;Object.assign(this.sun.shadow.camera,{left:-42,right:42,top:42,bottom:-42,near:1,far:240});this.sun.shadow.mapSize.set(1024,1024);this.sun.shadow.bias=-.0003;this.sun.shadow.normalBias=.07;this.scene.add(this.sun,this.sun.target);
  this.static=g();this.animated=[];this.stations=new Map();this.enemyViews=new Map();this.effects=new Map();this.pickups=new Map();this.shots=new Map();this.buildTerrain();this.buildLandscape();this.buildAnchorage();this.buildStations();buildAuthoredWorld(this.static,this.scene,this.animated);batch(this.static,this.scene);
  this.player=makeHero(game.player.weapon);this.weapon=game.player.weapon;this.scene.add(this.player);for(const e of game.enemies){const mesh=makeEnemy(e.type),danger=g(this.scene);for(let i=0;i<(e.type==='harrow'?5:1);i++){const lane=g(danger),mark=new T.Mesh(new T.PlaneGeometry(1,1),new T.MeshBasicMaterial({color:e.type==='harrow'?0x86dfdf:0xf3774c,side:T.DoubleSide,transparent:true,opacity:.24,depthWrite:false}));mark.rotation.x=-Math.PI/2;lane.add(mark);}this.scene.add(mesh);const health=g(this.scene),back=new T.Mesh(new T.PlaneGeometry(1.6,.12),new T.MeshBasicMaterial({color:0x27392e,depthTest:false})),fill=new T.Mesh(new T.PlaneGeometry(1.5,.06),new T.MeshBasicMaterial({color:0xd6bc82,depthTest:false}));health.add(back,fill);health.renderOrder=9;back.renderOrder=9;fill.renderOrder=10;fill.position.z=.01;this.enemyViews.set(e.id,{mesh,danger,health,fill});}

  this.target=g(this.scene);part(this.target,'cone',0xffc377,0,0,0,.23,.55,.23,[Math.PI,0,0]);this.look=new T.Vector3(0,40,0);this.camera.position.set(80,155,160);this.resize();this.setQuality(game.progress.quality);
 }
 resize(){this.camera.aspect=innerWidth/innerHeight;this.camera.updateProjectionMatrix();this.renderer.setSize(innerWidth,innerHeight,false);}
 setQuality(q){this.renderer.setPixelRatio(Math.min(devicePixelRatio||1,q==='low'?1:q==='high'?2:1.4));this.renderer.shadowMap.enabled=q!=='low';this.renderer.setSize(innerWidth,innerHeight,false);}
 buildTerrain(){
  const geo=new T.SphereGeometry(RADIUS,192,128).toNonIndexed(),pos=geo.attributes.position,colors=[];const ash=new T.Color(0x7c8667),sand=new T.Color(0xb5b18b),road=new T.Color(0x89886a),dark=new T.Color(0x506855);
  for(let i=0;i<pos.count;i++){V.fromBufferAttribute(pos,i).normalize();const a=Math.acos(clamp(V.y,-1,1)),d=a*RADIUS,ang=Math.atan2(V.x,V.z),x=Math.sin(ang)*d,z=Math.cos(ang)*d,h=a<1.4?landHeight(x,z):-.9;V.multiplyScalar(RADIUS+h);pos.setXYZ(i,V.x,V.y,V.z);const c=ash.clone().lerp(dark,clamp((Math.sin(x*.12)+Math.cos(z*.13))*.2+.2,0,.6));if(h<.8)c.copy(sand);else if(roadDistance(x,z)<1.7)c.copy(road);if(z<-26)c.lerp(dark,.3);c.multiplyScalar(.97+.03*Math.sin(x*.7+z*.5));colors.push(c.r,c.g,c.b);}
  geo.setAttribute('color',new T.Float32BufferAttribute(colors,3));geo.computeVertexNormals();const earth=new T.Mesh(geo,new T.MeshStandardMaterial({vertexColors:true,roughness:1,flatShading:true}));earth.receiveShadow=true;this.scene.add(earth);const sea=new T.Mesh(new T.SphereGeometry(RADIUS-.15,128,96),mat(0x507f78,{roughness:.43,metalness:.3}));sea.receiveShadow=true;this.scene.add(sea);

 }
 buildLandscape(){const rng=random(813);
  for(const d of DECORATIONS){const o=g(this.static);place(o,d.x,d.z,0,d.yaw);o.scale.setScalar(d.scale);if(d.kind==='spire'){
   for(let k=0;k<3;k++){const m=part(o,'rock',k%2?0x606a59:0x49594d,k*.12,.65+k*.58,-k*.06,.76-k*.09,.76,.66-k*.07,[0,k*.13,0]);}part(o,'rock',0x95967c,.11,2.08,-.08,.56,.19,.49);
  }else{rod(o,[0,0,0],[.4,2.1,0],.13,0x555443);rod(o,[.2,1.2,0],[-.7,2.3,.1],.07,0x555443);rod(o,[.3,1.7,0],[1.2,2.5,-.2],.06,0x555443);for(const [x,y,z,w]of [[-.6,2.3,.1,.9],[.3,2.65,0,1.15],[1.2,2.6,-.2,.8]])part(o,'soft',z<0?0x707950:0x566c4d,x,y,z,w,.45,w*.65);}}

  for(let i=0;i<700;i++){const x=(rng()-.5)*100,z=(rng()-.5)*105;if(landHeight(x,z)<.6||roadDistance(x,z)<2.2)continue;const o=g(this.static);place(o,x,z,0,rng()*6.28);part(o,'cone',i%5?0x9b7860:0xbc8c53,0,.2,0,.07,.45+rng()*.3,.11,[0,0,.3]);}
  for(let i=0;i<16;i++){const a=i*2.4,d=78+rng()*50,o=g(this.scene);place(o,Math.sin(a)*d,Math.cos(a)*d,9+rng()*6);for(let j=0;j<4;j++)part(o,'soft',i%2?0x71808c:0x8d9091,(j-1.5)*4,rng()*1.2,0,5,1.5,3);o.traverse(m=>m.castShadow=false);this.animated.push({type:'cloud',mesh:o,base:o.position.clone(),phase:i});}
  for(let i=0;i<14;i++){const a=i*2.3,d=110+i*3,o=g(this.static);place(o,Math.sin(a)*d,Math.cos(a)*d,1);part(o,'ball',0x475c67,0,0,0,4+i%4,1.8,3+i%3);part(o,'cone',0x41535c,0,2,0,1.5,5,2);}
  // Wind-torn crew flags mark roads, paired with amber lamps.
  for(const [x,z]of [[-4,12],[8,22],[-19,4],[17,-8],[23,19],[-5,-20]]){const o=g(this.static);place(o,x,z);part(o,'cyl',0x9e8b6a,0,2.5,0,.07,5,.07);const flag=g(this.scene);place(flag,x,z,4.1);for(let j=0;j<5;j++)part(flag,'box',j%2?0xb35d3f:0xd17549,.27+j*.27,-j*.05,0,.3,.8-j*.08,.04);this.animated.push({type:'flag',mesh:flag});glow(o,'box',0xffae62,0,2.1,.18,.18,.32,.1);}
 }
 buildAnchorage(){
  buildHomes(this.static);
  const engine=g(this.static);place(engine,0,19);part(engine,'cyl',0x384e5c,0,.5,0,2,.7,2);part(engine,'box',0xb19469,0,1.35,0,1.5,1.3,1.4);const wheel=g(this.scene);place(wheel,0,19,2.6);for(let i=0;i<8;i++){const a=i*Math.PI/4;part(wheel,'box',0xb69663,Math.sin(a)*1.3,Math.cos(a)*1.3,0,.3,.55,.2,[0,0,-a]);}part(wheel,'ring',0x557a8b,0,0,0,1.3);glow(wheel,'ball',0x8fe0df,0,0,0,.4);this.animated.push({type:'wheel',mesh:wheel,baseQ:wheel.quaternion.clone()});
  const ship=g(this.static);place(ship,-23,34,.6,-.5);part(ship,'ball',0x2b4658,0,2,0,3.4,2.1,7);part(ship,'box',0xb18b58,0,3.3,0,3.7,.35,8);part(ship,'box',0x485f6d,0,4.2,-1,2.7,1.6,3);for(const s of [-1,1])part(ship,'box',0xa84e34,s*3.9,2.4,-1,5,.16,3,[0,0,s*.2]);rod(ship,[0,3.5,2],[.4,8,1.2],.1,0xc6ad77);part(ship,'box',0xc08051,.6,6.7,1.2,2.2,2.5,.07,[0,.2,-.2]);
  const rescueShip=g(this.scene);place(rescueShip,19,32,7,-.5);part(rescueShip,'ball',0x3a687c,0,0,0,1.8,1,4);part(rescueShip,'box',0xbb9563,0,.8,0,2,.2,4.7);for(const side of [-1,1])part(rescueShip,'box',0xc16945,side*2,.2,0,2.4,.13,1.8,[0,0,side*.12]);glow(rescueShip,'box',0x99e8df,0,1,-1,1.3,.2,.6);this.animated.push({type:'crewship',mesh:rescueShip,base:rescueShip.position.clone()});
  for(const [x,z,npc]of [[-7,20,true],[-23,0,true],[-4,29,true]]){const mesh=makeHero('shear',npc);place(mesh,x,z,0,1);this.scene.add(mesh);if(x===-4){mesh.userData.scarf.children.forEach(m=>m.material=mat(0x45aeb2));part(mesh.userData.body,'box',0x96764a,0,1.5,-.5,.85,.9,.4);rod(mesh.userData.body,[.3,1.8,-.5],[.3,3.5,-.5],.035,0xc3b892);}this.animated.push({type:'npc',mesh,x,z});const signal=g(this.scene);place(signal,x,z,3.6);part(signal,'box',0xefbd7a,0,0,0,.24,.4,.24,[0,0,Math.PI/4]);this.animated.push({type:'signal',mesh:signal,base:signal.position.clone()});}
 }
 buildStations(){for(const l of LANDMARKS){const o=g(this.static);place(o,l.x,l.z);
  if(l.type==='relay'){
   part(o,'box',0x345366,0,.2,0,5,.4,5);for(const side of [-1,1]){rod(o,[side*1.5,0,-.8],[side*.5,6,-.8],.13,0xa48b62);rod(o,[side*1.4,1,-.8],[-side*.9,4,-.8],.07,0x6b8896);}part(o,'box',0xb49468,0,1.15,.9,1.7,1.5,.5);const dish=g(this.scene);place(dish,l.x,l.z,6.3);part(dish,'ring',0x819da9,0,0,0,1.7);for(let i=0;i<4;i++){const a=i*Math.PI/2;rod(dish,[0,0,0],[Math.sin(a)*1.6,Math.cos(a)*1.6,0],.07,0xaebca8);}glow(dish,'ball',0x81e7e5,0,0,.05,.3);this.animated.push({type:'relay',mesh:dish,id:l.id});
   for(let i=0;i<4;i++){const span=g(this.static);place(span,l.x+(i-1.5)*2.2,l.z+4.5);part(span,'box',0x567584,0,.3,0,1.6,.4,1.1,[0,0,(i-1.5)*.08]);}
  }else if(l.type==='station'){part(o,'box',0x405561,0,.25,0,4.6,.5,4.3);part(o,'box',0x9c825e,0,1.1,0,1.5,1.2,1.1);for(const s of [-1,1]){rod(o,[s*1.8,0,-1],[s*1.8,4,-1],.14,0x657b83);part(o,'box',0xb29972,s*1.8,3.9,-1,.5,.35,.5);}rod(o,[-1.8,4,-1],[1.8,4,-1],.16,0x70858a);const core=g(this.scene);place(core,l.x,l.z,2);part(core,'box',0xd4b073,0,0,0,.6,.9,.35,[0,0,.3]);glow(core,'box',0x9de8e5,0,0,.21,.12,.66,.05);this.stations.set(l.id,core);
   if(l.id==='north'){for(const s of [-1,1])part(o,'box',0x344c5c,s*4,4,-3,1.3,8,1.5,[0,0,s*.1]);part(o,'box',0xa58b66,0,8.2,-3,8,.6,1.7,[0,0,.1]);}
   if(l.id==='west'){for(let i=0;i<4;i++)part(o,'rock',0x53604f,-3+i*2,1.3,-3,1,2.6,1.1,[.1,0,(i-2)*.12]);}
   if(l.id==='east'){part(o,'cyl',0xa88452,0,.6,-3,1.1,5,1.1,[0,0,Math.PI/2]);for(const s of [-1,1])part(o,'ring',0x354e5a,s*2.5,.6,-3,1.1,1.1,1.1,[0,Math.PI/2,0]);}
  }else if(l.type==='bench'){part(o,'box',0xa98455,0,.9,0,2.3,.25,1.2);for(const s of [-1,1])part(o,'box',0x334a56,s*.9,.4,0,.2,.8,1);part(o,'box',0x6d8a97,0,1.25,0,.8,.5,.65);glow(o,'box',0x88dbdb,0,1.55,0,.3,.06,.3);}
  else if(l.type==='cache'){const box=g(this.scene);place(box,l.x,l.z);part(box,'box',0x3e5868,0,.45,0,1.4,.85,1);part(box,'box',0xad8c60,0,.85,0,1.5,.16,1.1);for(const s of [-1,1])part(box,'box',0xd2ae72,s*.44,.48,.53,.11,.72,.07);glow(box,'box',0x8adfdf,0,.5,.56,.25,.12,.04);this.animated.push({type:'cache',mesh:box,id:l.id});}
  else if(l.type==='chart'){rod(o,[0,0,0],[0,1.5,0],.12,0x8b7759);part(o,'box',0x344d5f,0,1.65,0,1.7,1,.12,[.2,0,0]);part(o,'box',0xcda973,0,1.7,.1,1.3,.06,.05);}
  else if(l.type==='boss'){for(let i=0;i<12;i++){const a=i*Math.PI/6,p=g(this.static);place(p,l.x+Math.sin(a)*12,l.z+Math.cos(a)*12,0,-a);part(p,'box',0x3a5363,0,1.4,0,1.5,2.8,1.8);part(p,'box',0xbb945d,0,2.9,0,1.8,.3,2);glow(p,'box',0xffb66b,0,1.6,1,.5,.16,.05);}for(let i=0;i<5;i++){const a=g(this.static);place(a,l.x+(i-2)*3.3,l.z-9);part(a,'box',0x4a5a49,0,3.5,-1,.7,7+(i%2)*2,1.4,[0,0,(i-2)*.2]);part(a,'box',0x9e704b,.6,6.8,-1,2,.4,1.5,[0,0,(i-2)*.15]);}}
 }}
 orientCameraMove(x,z){return {x:x*Math.cos(this.yaw)+z*Math.sin(this.yaw),z:-x*Math.sin(this.yaw)+z*Math.cos(this.yaw)};}
 animateHero(mesh,p,time){place(mesh,p.x,p.z,.02,p.yaw);const d=mesh.userData,b=d.body,moving=p.move>0&&!p.attack,speed=moving?Math.min(1,p.move):0,phase=time*12;
 b.position.y=Math.abs(Math.sin(phase))*speed*.08;b.rotation.set(0,0,0);d.head.rotation.y=Math.sin(time*.7)*.035;
 for(let i=0;i<2;i++){const leg=d.legs[i],s=Math.sin(phase+i*Math.PI);leg.rotation.x=s*speed*.58;leg.userData.shin.rotation.x=Math.max(0,-s)*speed*.65;}
 d.armL.rotation.set(-Math.sin(phase)*speed*.27,0,.12);d.armR.rotation.set(Math.sin(phase)*speed*.27,0,-.12);d.armR.position.z=0;d.armL.userData.elbow.rotation.x=-.18;d.armR.userData.elbow.rotation.x=-.25;
 if(p.attack){const a=p.attack,wind=Math.min(1,a.age/a.windup),t=Math.max(0,(a.age-a.windup)/(a.duration-a.windup)),snap=1-Math.pow(1-Math.min(1,t*4),3),relax=Math.max(0,(t-.35)/.65);b.position.y-=.12*(a.age<a.windup?wind:1-relax);
 if(a.weapon==='shear'){const side=a.combo%2?-1:1,bias=a.age<a.windup?-wind: -1+snap*2;b.rotation.y=side*bias*.85*(1-relax*.65);d.armR.rotation.set(.7,side*bias*.9,-.65);d.armR.userData.elbow.rotation.x=-.15;d.armL.rotation.x=-.8;d.legs[0].rotation.x=-.25;d.legs[1].rotation.x=.3;if(a.combo===2&&!a.vent){b.rotation.y=a.age<a.windup?-wind*.7:-.7+snap*Math.PI*2;d.armR.rotation.z=-1.1;}}
 if(a.weapon==='pike'){d.armR.rotation.x=a.age<a.windup?.4:.55;d.armR.position.z=a.age<a.windup?-.3*wind:1.05*snap*(1-relax);d.armR.userData.elbow.rotation.x=a.age<a.windup?-1.1*wind:-1.1+snap*1.3;b.rotation.y=-.4*(1-snap);b.rotation.x=.15*snap*(1-relax);}
 if(a.weapon==='maul'){d.armR.rotation.x=a.age<a.windup?-2.3*wind:-2.3+snap*3.2;d.armR.userData.elbow.rotation.x=a.age<a.windup?-.6*wind:-.6+snap*.8;d.armL.rotation.x=d.armR.rotation.x*.7;b.rotation.x=a.age<a.windup?-.25*wind:.4*snap*(1-relax);}}
 if(p.dash>0){b.rotation.x=.8;b.position.y=-.35;d.legs[0].rotation.x=-.7;d.legs[1].rotation.x=.85;d.armL.rotation.x=1;}
 const trailing=moving?.35:p.dash>0?.7:.08;d.scarf.rotation.x=-trailing;d.scarf.rotation.y=Math.sin(time*4)*.07-b.rotation.y*.2;for(const [i,c]of d.coat.entries()){c.rotation.x=.15+Math.sin(time*6+i)*.08+trailing;c.rotation.y=Math.sin(time*4+i)*.08;}mesh.visible=true;
 }

 update(dt){this.elapsed+=dt;const time=this.elapsed,p=this.game.player;
  if(this.weapon!==p.weapon){this.scene.remove(this.player);this.player=makeHero(p.weapon);this.scene.add(this.player);this.weapon=p.weapon;}this.animateHero(this.player,p,time);
  for(const e of this.game.enemies){const {mesh,danger,health,fill}=this.enemyViews.get(e.id);mesh.visible=e.alive&&(!e.relay||this.game.active(e))&&(e.type!=='harrow'||this.game.active(e));if(mesh.visible){place(mesh,e.x,e.z,.05,e.yaw);const d=mesh.userData;d.body.position.y=['kite','harrow'].includes(e.type)?Math.sin(time*3+e.id)*.22:0;if(d.rotor)d.rotor.rotation.z=time*(e.state==='windup'?9:3);for(let i=0;i<d.legs.length;i++)d.legs[i].rotation.x=e.state==='idle'?Math.sin(time*9+i)*.17:0;d.body.rotation.x=e.state==='windup'?-.42*(1-e.timer/ENEMIES[e.type].windup):e.state==='stagger'?.15:0;d.body.rotation.z=e.state==='stagger'?Math.sin(time*20)*.025:0;d.body.position.z=-(e.recoil||0)*1.6;d.body.rotation.x+=(e.recoil||0)*1.2;}
   health.visible=mesh.visible&&e.hp<ENEMIES[e.type].hp&&Math.hypot(e.x-p.x,e.z-p.z)<15&&!['engine','harrow'].includes(e.type);if(health.visible){health.position.copy(surface(e.x,e.z,e.type==='dredger'?2.9:2.5));health.quaternion.copy(this.camera.quaternion);const ratio=Math.max(0,e.hp/ENEMIES[e.type].hp);fill.scale.x=ratio;fill.position.x=-(1-ratio)*.75;}
   danger.visible=this.game.active(e)&&e.state==='windup';if(danger.visible){const s=ENEMIES[e.type],offsets=e.type==='harrow'?(e.hp<s.hp*.5?[-.6,-.3,0,.3,.6]:[-.3,0,.3]):[0];place(danger,e.attackX,e.attackZ,.1,e.attackYaw);danger.children.forEach((lane,i)=>{lane.visible=i<offsets.length;lane.rotation.y=offsets[i]||0;const mark=lane.children[0];mark.position.z=s.reach/2;mark.scale.set(s.width*2,s.reach,1);mark.material.opacity=.15+.28*(1-e.timer/s.windup);});}}

  for(const [id,mesh]of this.stations){mesh.visible=!this.game.progress.teeth.includes(id);mesh.rotateY(dt*.7);}
  for(const a of this.animated){if(a.type==='npc'){a.mesh.userData.head.rotation.y=Math.sin(time*.7+a.x)*.12;a.mesh.userData.body.position.y=Math.sin(time*1.7+a.x)*.02;}if(a.type==='cloth')a.mesh.rotation.z=Math.sin(time*2+a.phase)*.045;if(a.type==='bird'){const angle=time*.065+a.phase;placeArt(a.mesh,Math.cos(angle)*53,Math.sin(angle)*57,12+Math.sin(angle*2)*3,-angle);a.mesh.children.forEach((wing,i)=>wing.rotation.z=(i?1:-1)*(.15+Math.sin(time*4+a.phase)*.25));}if(a.type==='cloud')a.mesh.position.copy(a.base).addScaledVector(new T.Vector3(1,0,0),Math.sin(time*.04+a.phase)*2);if(a.type==='flag')a.mesh.children.forEach((m,i)=>m.rotation.y=Math.sin(time*4-i*.6)*.2);if(a.type==='wheel')a.mesh.quaternion.copy(a.baseQ).multiply(new T.Quaternion().setFromAxisAngle(new T.Vector3(0,0,1),time*.35));if(a.type==='signal')a.mesh.position.copy(a.base).addScaledVector(a.base.clone().normalize(),Math.sin(time*2)*.12);if(a.type==='relay'){a.mesh.rotation.z=Math.sin(time*(this.game.progress.relays.includes(a.id)?1.8:.25))*.15;}if(a.type==='crewship'){a.mesh.visible=this.game.progress.rescueDone;a.mesh.position.copy(a.base).addScaledVector(a.base.clone().normalize(),Math.sin(time)*.3);}if(a.type==='cache')a.mesh.visible=!this.game.progress.caches.includes(a.id);}
  const q=this.game.quest();this.target.visible=!!q.target&&this.started&&!this.overview;if(q.target)place(this.target,q.target.x,q.target.z,5+Math.sin(time*2)*.2);
  this.syncEffects();this.syncItems(this.game.pickups,this.pickups,false);this.syncItems(this.game.shots,this.shots,true);this.updateCamera(dt);this.renderer.render(this.scene,this.camera);
 }
 createEffect(f){const o=g(this.scene),color=WEAPONS[f.weapon]?.color||0xffc17d;
  if(f.shape==='impact'){const center=f.reach-f.width;for(let i=0;i<14;i++){const angle=i*2.4,r=f.width*(.4+(i%3)*.25);part(o,'rock',i%2?0x797455:0xbda279,Math.sin(angle)*r,.15+(i%3)*.17,center+Math.cos(angle)*r,.15,.3+(i%4)*.12,.18,[0,angle,.4]);}for(let i=0;i<6;i++)glow(o,'box',0xe3b260,Math.sin(i)*f.width*.5,.09,center+Math.cos(i)*f.width*.5,.07,.04,f.width,[0,i,0]);}
  else if(f.type==='strike'||f.type==='vent'){
   if(f.weapon==='shear'){o.add(crescent(color,f.reach,f.combo,f.width||1.15));if(f.type==='vent'){const c=crescent(0xffe3a4,f.reach*.85,0);c.rotation.z=.22;c.rotation.y=.22;o.add(c);}}
   else if(f.weapon==='pike'){glow(o,'cone',color,0,1.1,f.reach/2,.16,f.reach,.16,[Math.PI/2,0,0]);for(const s of [-1,1])part(o,'box',0xdbfaf4,s*.2,1.1,f.reach*.4,.025,.03,f.reach*.5);}
   else{for(let i=0;i<(f.type==='vent'?6:3);i++){const z=1+i*f.reach/(f.type==='vent'?6:3);part(o,'cone',0xe6a765,(i%2?1:-1)*.3,.2,z,.4,1.2-i*.1,.3,[.2,0,i%2?.3:-.3]);glow(o,'box',0xffca76,0,.1,z,.16,.05,1.3,[0,i%2?.3:-.3,0]);}}
  }else if(f.type==='enemy-strike'){for(let i=0;i<6;i++)part(o,'box',0xe9845d,(i%2?1:-1)*.5,.12,1+i*f.reach/6,.13,.15,1.2,[0,i%2?.5:-.5,0]);}
  else if(f.type==='dash'){for(let i=0;i<4;i++)glow(o,'box',0x9ce7ed,(i-1.5)*.16,.65,-i*.5,.06,.07,1.3);}
  else{const count=f.type==='shatter'?18:9;for(let i=0;i<count;i++){const a=i*2.4,m=part(o,'box',f.type==='repair'?0x8fdedf:i%2?0xffbc6d:0xe7ead7,Math.sin(a)*.2,1+Math.cos(a)*.2,Math.cos(a)*.2,.09,.09,.3);m.userData.baseScale=m.scale.clone();m.userData.velocity=new T.Vector3(Math.sin(a)*(2+i%3),2+i%4,Math.cos(a)*(2+i%2));}if(f.type==='break'||f.type==='perfect'){glow(o,'box',0xabf7f1,0,1,0,1.8,.06,.06,[0,0,.6]);glow(o,'box',0xabf7f1,0,1,0,1.8,.06,.06,[0,0,-.6]);}}
  o.traverse(m=>{m.castShadow=false;});return o;
 }
 syncEffects(){const ids=new Set();for(const f of this.game.effects){ids.add(f.id);let o=this.effects.get(f.id);if(!o){o=this.createEffect(f);this.effects.set(f.id,o);}place(o,f.x,f.z,.1,f.yaw||0);const age=1-f.life/f.maxLife;o.scale.setScalar(f.type==='impact'?1:1+age*.3);for(const m of o.children){if(m.userData.velocity){const v=m.userData.velocity;m.position.set(v.x*age,1+v.y*age-3*age*age,v.z*age);m.scale.copy(m.userData.baseScale).multiplyScalar(Math.max(.05,1-age));}if(m.material?.isMeshBasicMaterial)m.material.opacity=1-age;}}
  for(const [id,o]of this.effects)if(!ids.has(id)){this.scene.remove(o);o.traverse(m=>{if(m.material?.isMeshBasicMaterial){m.material.dispose();m.geometry.dispose();}});this.effects.delete(id);}}
 syncItems(items,map,shot){const ids=new Set();for(const p of items){ids.add(p.id);let o=map.get(p.id);if(!o){o=g(this.scene);glow(o,shot?'cone':'box',shot?0xffa465:0x9ae0dd,0,0,0,shot?.15:.2,shot?.65:.24,.18,shot?[Math.PI/2,0,0]:[0,0,Math.PI/4]);map.set(p.id,o);}place(o,p.x,p.z,shot?1.3:.8+Math.sin(this.elapsed*4)*.1,shot?Math.atan2(p.vx,p.vz):this.elapsed);}for(const [id,o]of map)if(!ids.has(id)){this.scene.remove(o);map.delete(id);}}
 updateCamera(dt){const p=this.game.player,center=surface(p.x,p.z,1.2),up=center.clone().normalize(),north=surface(p.x,p.z-1,1.2).sub(center).normalize(),east=new T.Vector3().crossVectors(north,up).normalize(),south=north.clone().negate(),offset=east.multiplyScalar(Math.sin(this.yaw)).addScaledVector(south,Math.cos(this.yaw));let desired,target;
  if(!this.started){desired=surface(12,42,23);target=surface(-3,7,2);this.camera.up.lerp(target.clone().normalize(),.08);}else if(this.overview){const a=this.yaw;desired=new T.Vector3(Math.sin(a)*140,155,Math.cos(a)*155);target=new T.Vector3(-8,32,0);this.camera.up.lerp(UP,.08);}
  else{const dist=(this.camera.aspect<.8?24:20)*this.zoom;desired=center.clone().addScaledVector(up,dist*.95).addScaledVector(offset,dist);target=center.clone().addScaledVector(offset,-2.2);this.camera.up.lerp(up,1-Math.exp(-dt*6)).normalize();}
  this.camera.position.lerp(desired,1-Math.exp(-dt*5));this.look.lerp(target,1-Math.exp(-dt*8));this.shake=Math.max(0,this.shake-dt*3);if(this.game.progress.shake&&this.shake>0)this.camera.position.add(new T.Vector3(Math.sin(this.elapsed*130),Math.cos(this.elapsed*157),0).multiplyScalar(this.shake*.25));this.camera.lookAt(this.look);this.sun.target.position.copy(center);this.sun.position.copy(center).add(new T.Vector3(-45,75,35));
 }
}
